# IntelNexus 用户使用指南

> 本指南面向非技术用户，无需编程知识即可使用。

---

## 一、首次使用（3 分钟）

### 步骤 1：解压文件
将下载的 `IntelNexus-xxxx.zip` 解压到任意文件夹（建议桌面或 D 盘）。

### 步骤 2：双击 `setup.bat`
- 等待自动安装（首次约 2-5 分钟）
- 看到 "Setup complete!" 即可关闭

### 步骤 3：双击 `run.bat`
- 浏览器会自动打开 http://localhost:8501
- 看到界面即成功！

---

## 二、配置 AI 模型（必须）

IntelNexus 需要 AI 模型才能工作。有两种方式：

### 方式 A：使用本地模型（推荐，免费）

1. 下载安装 Ollama：https://ollama.com
2. 安装后打开命令提示符，输入：
   ```
   ollama pull qwen2.5:7b
   ```
3. 回到 IntelNexus 界面，侧边栏会自动检测到模型

### 方式 B：使用在线模型（需要 API Key）

1. 在界面侧边栏点击「添加自定义模型」
2. 选择提供商（如 DeepSeek、通义千问等）
3. 粘贴 API Key（从对应官网获取）
4. 点击「测试连接」→「保存并启用」

---

## 三、配置邮件推送（可选）

如需接收定时简报邮件：

1. 进入「简报中心」→「订阅管理」
2. 展开「邮件设置」
3. 填写 SMTP 信息（以 163 邮箱为例）：
   - SMTP 服务器：`smtp.163.com`
   - 端口：`465`
   - 用户名：你的 163 邮箱
   - 密码：163 邮箱的**授权码**（不是登录密码！）
4. 点击「发送测试邮件」验证

> 💡 获取 163 授权码：登录 163 邮箱 → 设置 → POP3/SMTP/IMAP → 开启服务 → 生成授权码

---

## 四、日常使用

### 搜索情报
1. 在首页输入关键词
2. 选择搜索模式（网页/新闻/全部）
3. 点击搜索，等待结果

### 生成简报
1. 进入「简报中心」
2. 选择关注点（可多选）
3. 点击「生成简报」

### 设置定时推送
1. 进入「简报中心」→「订阅管理」
2. 添加订阅者（姓名 + 邮箱）
3. 设置推送时间和频率
4. 系统会按时自动发送

---

## 五、常见问题

### Q: 双击 run.bat 后浏览器没打开？
A: 手动打开浏览器，访问 http://localhost:8501

### Q: 提示 "Python not found"？
A: 需要安装 Python 3.10+，下载地址：https://www.python.org/downloads/
安装时务必勾选 "Add python.exe to PATH"

### Q: 模型连接失败？
A: 
- 本地模型：确认 Ollama 已启动（任务栏有图标）
- 在线模型：检查 API Key 是否正确、是否过期

### Q: 邮件发送失败？
A: 
- 检查 SMTP 密码是否为**授权码**（不是登录密码）
- 检查端口是否正确（465 或 587）
- 确认邮箱已开启 SMTP 服务

### Q: 如何更新版本？
A: 下载新版本 zip，解压覆盖旧文件，重新运行 setup.bat

---

## 六、获取帮助

- 项目地址：https://github.com/yuanyyyyyyyyy/IntelNexus
- 问题反馈：在 GitHub 提交 Issue

---

## 附录：配置项说明

| 配置项 | 说明 | 是否必填 |
|--------|------|----------|
| AI 模型 | 用于分析情报 | 是 |
| SMTP 设置 | 用于邮件推送 | 否 |
| 企业微信 Webhook | 用于企微推送 | 否 |
| 钉钉 Webhook | 用于钉钉推送 | 否 |
