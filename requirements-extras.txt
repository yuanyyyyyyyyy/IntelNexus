# IntelNexus 可选扩展依赖
# 安装方式: pip install -r requirements-extras.txt
# 不安装也能正常运行全部核心功能；缺失时相关功能自动降级：
#   - langchain-anthropic / langchain-google-genai : Anthropic 与 Gemini 云端模型接入
#     （未安装时侧边栏添加模型请选 OpenAI 兼容类型）
#   - sentence-transformers : 语义可信度评分增强（国内需 HuggingFace 镜像，
#     默认 DISABLE_SENTENCE_TRANSFORMER=true 已禁用，不装等效）
#   - spacy + 语言模型 : 知识图谱的实体抽取增强（未安装时用规则分词降级）
#     spacy 模型需额外执行:
#       python -m spacy download zh_core_web_sm
#       python -m spacy download en_core_web_sm

langchain-anthropic>=0.1.0
langchain-google-genai>=0.0.5
sentence-transformers>=2.2.0
spacy>=3.6.0

# 搜索结果图表可视化（懒加载，未安装时相关图表面板降级）
matplotlib>=3.7.0

# ---------------------------------------------------------------------------
# 论文 A 实验管线依赖（experiments/）
# 与业务依赖隔离：不安装不影响 IntelNexus 主程序运行，
# 但对应实验指标会按纪律保留 not-yet-measured，不做近似替代。
#   - PyYAML       : 读取 experiments/configs/*.yaml
#   - scipy        : 表7 的 Wilcoxon 符号秩检验
#   - psutil       : 无 GPU 时的进程内存峰值采样
#   - pynvml       : 显存采样（nvidia-smi 不可用时的回退）
#   - bert-score   : 表3 的 BERTScore。基座用 bert-base-multilingual-cased
#                    （生成物中文、参考摘要多为英文，必须跨语言基座）。
#                    基座不由此文件安装，而是固定落盘：
#                    `python -m experiments.cli bertscore-fetch`
#                    （多源直连 + 断点续传；国内 huggingface.co 不可达，
#                     默认走 ModelScope 与 hf-mirror，见 experiment.yaml 的
#                     evaluation.bertscore.sources）
#   - rouge-score  : ROUGE-L 的交叉校验实现（主实现为仓库内自研，不强制）
# ---------------------------------------------------------------------------
PyYAML>=6.0
scipy>=1.10.0
psutil>=5.9.0
pynvml>=11.5.0
bert-score>=0.3.13
rouge-score>=0.1.2
